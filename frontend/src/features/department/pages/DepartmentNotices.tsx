import { Notices } from "../../../pages/admin/Notices";

// We re-use the extremely capable Admin Notices view for the department because it already targets specific users cleanly.
export function DepartmentNotices() {
  return <Notices />;
}
